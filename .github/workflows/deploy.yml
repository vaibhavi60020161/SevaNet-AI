name: Vercel Auto-Deployment Status

on:
  push:
    branches: ["main"]

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Acknowledge Vercel Deployment
        run: echo "Vercel auto-deploys on every push to main. GitHub Pages is disabled to avoid API failures."
